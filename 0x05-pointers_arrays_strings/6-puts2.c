#include "main.h"

/**
 * puts2 - prints every other character
 * @str: string
*/

void puts2(char *str)
{
	int i;

	i = 0;

	while (str[i] != '\0')
	{
		(if % 2 == 0)
			_putchar(str[i]);
		i++;
	}
	_putchar('\n');
}
